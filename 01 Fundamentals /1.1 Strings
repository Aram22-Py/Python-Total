#printing strings in new lines and using \ as marker to tell the compiler the next character is a string
print("Line 1 \n Line 2 \n Line 3")

#Also receiving information from user using the function input
input("Cuál es tu nombre?")
#And we  can print what the user wrote with
print ( input("Cual es tu nombre?" ) )
